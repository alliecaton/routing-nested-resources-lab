<h1>Artists</h1>
<ul>
  <% @artists.each do |artist| %>
    <li><%= link_to artist.name, artist_path(artist) %></li>
    <%= link_to "#{artist.name} songs", artist_songs_path(artist.id) %>
  <% end %>
</ul>
